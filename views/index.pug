extends layout

block content
  h1= title
  p Welcome to  Bhavya's index page  #{title}
  button(type="button", class="btn btn-secondary", onclick="update()") Press Me
  <br>
  <br>
  div(id="target") Display
  <br>
  a(href = '/fairydoll') Fairy Doll Page link
  |
  script. 
    let client_count = 0;
    function update() {
      client_count = client_count + 1;
      document.getElementById("target").innerHTML = `Value now ${client_count}`  ;
      }
